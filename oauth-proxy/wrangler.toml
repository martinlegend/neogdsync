name = "neogdsync-oauth"
main = "worker.js"
compatibility_date = "2026-05-01"

# Free workers.dev subdomain — HTTPS included automatically.
# Deployed URL: https://neogdsync-oauth.<your-cf-subdomain>.workers.dev
#
# Secrets (never stored here — inject via CLI):
#   npx wrangler secret put GOOGLE_CLIENT_ID
#   npx wrangler secret put GOOGLE_CLIENT_SECRET
